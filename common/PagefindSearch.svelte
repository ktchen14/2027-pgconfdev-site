<!-- Rendered by /pagefind/pagefind-component-ui.js (loaded via site.html). The
     custom elements are undefined until that script loads (e.g. in dev, which
     serves an empty stub), in which case they render nothing. The script is a
     module, so its bundle path can't be auto-detected; set it explicitly. -->
<pagefind-config bundle-path="/2027-pgconfdev-site/pagefind/"></pagefind-config>
<pagefind-modal-trigger></pagefind-modal-trigger>
<pagefind-modal reset-on-close></pagefind-modal>
